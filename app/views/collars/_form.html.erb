<%= form_for(@collar) do |f| %>
  <% if @collar.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@collar.errors.count, "error") %> prohibited this collar from being saved:</h2>

      <ul>
      <% @collar.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :collarable_id %><br>
    <%= f.text_field :collarable_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
